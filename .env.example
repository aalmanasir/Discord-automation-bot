# Discord bot token — obtain from https://discord.com/developers/applications
DISCORD_TOKEN=your-bot-token-here

# Absolute path to the local git repository that /openclaw will push.
# Defaults to "." (the directory where the bot is started) when not set.
OPENCLAW_REPO_PATH=/path/to/your/repo
